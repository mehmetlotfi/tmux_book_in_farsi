### **[بخش 02: پیشگفتار](02-پیشگفتار.md)**

### سرفصل‌های این کتاب

#### **[بخش ۱: سخن مولف](01-سخن_مولف.md)**
- تعهد به فلسفهٔ نرم‌افزار آزاد  
- معرفی نرم‌افزار GNU TeXMacs و مجوزهای آن (GNU GPL نسخهٔ ۳)  
- مجوز انتشار کتاب (GNU Free Documentation License نسخهٔ ۱٫۳ و بالاتر)  
- اهمیت بیان ابزارهای مورد استفاده در تولید اثر علمی  
- چهار آزادی اساسی در نرم‌افزار آزاد  
- شفافیت، همکاری و پیشرفت جمعی  
- مفهوم Copyleft و جلوگیری از انحصارگرایی مجدد  
- تعهد روش‌شناختی مؤلف به استفاده از بسترهای آزاد و غیرانحصاری  

---

#### **[بخش ۲: پیشگفتار](02-پیشگفتار.md)**
- مشکل جابه‌جایی بین برنامه‌ها با ماوس در برابر استفاده از شورت‌کات‌های صفحه‌کلید  
- چالش کار با چند برنامه به‌صورت هم‌زمان روی سرور از طریق SSH  
- خطر قطع اتصال SSH و هدر رفتن پردازش‌های طولانی  
- معرفی ابزارهای `screen` و `tmux` به‌عنوان راه‌حل  
- هدف کتاب: آموزش جامع `tmux`  
- مزایای استفاده از tmux:  
  - مدیریت چندین برنامه در یک محیط واحد  
  - تقسیم پنجره به بخش‌های مختلف (پنل)  
  - اجرای هم‌زمان چند برنامه  
  - جابه‌جایی سریع با صفحه‌کلید و بدون ماوس  
  - افزایش بهره‌وری و تمرکز  
- آنچه در این کتاب یاد خواهید گرفت:  
  - پیکربندی، استفاده و سفارشی‌سازی tmux  
  - مدیریت چندین برنامه به‌صورت هم‌زمان  
  - نوشتن اسکریپت برای ساخت محیط‌های سفارشی  
  - استفاده از tmux برای کار از راه دور با دیگران  
- پیش‌فرض‌های کتاب: آشنایی با ترمینال، نحوهٔ نصب و حذف برنامه‌ها  
- سیستم‌عامل مورد استفاده در مثال‌ها: Ubuntu 24.04  

---

#### بخش ۳: سرفصل‌های کتاب (بخش فعلی)

---
#### **[بخش ۴: نصب tmux به روش‌های مختلف](04-نصب_تی_ماکس_به_روش_های_مختلف.md)**
- **پیش‌نیازهای سخت‌افزاری و سیستمی**  
  - سیستم‌عامل‌های پشتیبانی‌شده (macOS، یونیکس، لینوکس)  
  - عدم اجرای مستقیم روی ویندوز و راه‌حل‌های جایگزین (WSL، ماشین مجازی، psmux)  

- **روش اول: نصب با استفاده از پکیج‌منیجر سیستم‌عامل**  
  - نیاز به دسترسی `root` یا `superuser`  
  - دستور نصب در توزیع‌های مبتنی بر دبیان (`apt-get install tmux`)  
  - دستور نصب در توزیع‌های RedHat مانند فدورا (`dnf install tmux`)  
  - دستور نصب در توزیع‌های مبتنی بر ARCH (`pacman -S tmux`)  
  - دستور نصب در macOS با Homebrew (`brew install tmux`)  

- **روش دوم: کامپایل از سورس و نصب**  
  - دانلود سورس از مخزن گیت‌هاب (`git clone https://github.com/tmux/tmux.git`)  
  - ورود به پوشهٔ سورس  
  - بررسی پیش‌نیازهای کامپایل:  
    - libevent 2.x  
    - ncurses  
    - کامپایلر C (gcc یا clang)  
    - make، pkg-config و yacc (yacc یا bison)  
  - نصب پیش‌نیازها در دبیان (`apt-get install ...`)  
  - اجرای `sh autogen.sh` برای بررسی پیش‌نیازها  
  - ساخت Makefile با `./configure`  
  - کامپایل برنامه با دستور `make`  
  - شناسایی فایل باینری `tmux`  
  - اضافه کردن باینری به `PATH` با استفاده از `sudo make install`  
  - تأیید نصب با اجرای `tmux`  

- **نکته:** امکان نصب از ریپوزیتوری توزیع به‌جای کامپایل از سورس  

- **پایان بخش ۳**

---
#### **[بخش ۵: کار با tmux](05-کار_با_تی_ماکس.md)**
#### 

- **اجرای tmux**  
  - دستور سادهٔ `tmux` برای شروع یک نشست  
  - خروج از نشست با دستور `exit`  

- **ساختن نشست‌های اسم‌دار**  
  - دستور `tmux new -s <name>`  
  - اهمیت نام‌گذاری نشست‌ها برای مدیریت بهتر  

- **پیشوند فرمان**  
  - مفهوم `Ctrl+B` به‌عنوان پیشوند فرمان  
  - نحوهٔ صحیح استفاده (فشردن هم‌زمان و سپس رها کردن)  

- **جدا شدن از نشست (Detach)**  
  - کلید میان‌بر `Ctrl+B` و سپس `d`  

- **فهرست کردن نشست‌های موجود**  
  - دستور `tmux list-sessions` یا `tmux ls`  

- **اتصال مجدد به نشست‌های موجود**  
  - دستور `tmux attach -t <name-or-number>`  
  - کاربرد فوق‌العاده: mirror کردن صفحه بین چند کاربر از طریق SSH  

- **از بین بردن نشست‌ها**  
  - دستور `tmux kill-session -t <name>`  
  - خروج با `exit` درون نشست  

- **کار با پنجره‌ها (Windows)**  
  - مفهوم پنجره‌ها به‌عنوان تب در ترمینال  
  - ایجاد نشست با پنجرهٔ نام‌گذاری‌شده (`tmux new -s windows -n shell`)  
  - ایجاد پنجرهٔ جدید با `Ctrl+B` و `c`  
  - تغییر نام پنجره با `Ctrl+B` و `,` (کاما)  
  - جابه‌جایی بین پنجره‌ها:  
    - `Ctrl+B` و `n` (بعدی)  
    - `Ctrl+B` و `p` (قبلی)  
    - `Ctrl+B` و `0-9` (انتخاب با شماره)  
    - `Ctrl+B` و `f` (جستجو با اسم)  
    - `Ctrl+B` و `w` (نمایش لیست تعاملی)  
    - `Ctrl+B` و `s` (نمایش لیست نشست‌ها)  
  - بستن پنجره با `exit` یا `Ctrl+B` و `&`  

- **کار با پنل‌ها (Panes)**  
  - تقسیم عمودی با `Ctrl+B` و `%`  
  - تقسیم افقی با `Ctrl+B` و `"`  
  - چرخش بین پنل‌ها با `Ctrl+B` و `o`  
  - جابه‌جایی با کلیدهای جهت‌نما (`Ctrl+B` و فلش‌ها)  
  - تغییر چیدمان پنل‌ها با `Ctrl+B` و `Space`  
  - تغییر اندازهٔ پنل‌ها با `Ctrl+B` و `Ctrl+arrow`  
  - زوم روی پنل فعلی با `Ctrl+B` و `z`  
  - نمایش شمارهٔ پنل‌ها با `Ctrl+B` و `q`  
  - بستن پنل با `exit` یا `Ctrl+B` و `x`  

- **اسکرول و جستجو در پنل**  
  - اسکرول با `Ctrl+B` و `PageUp/PageDown`  
  - جستجو با `Ctrl+S` در حالت اسکرول  

- **کار با حالت فرمان (Command Mode)**  
  - ورود به حالت فرمان با `Ctrl+B` و `:` (دو نقطه)  
  - اجرای دستورات مستقیم مانند:  
    - `new-window -n console`  
    - `new-window -n processes "top"`  
  - کاربرد حالت فرمان برای ساختن پنجره، پنل یا نشست جدید و تنظیمات پیشرفته  

- **مشاهدهٔ کلیدهای میان‌بر**  
  - `Ctrl+B` و `?` برای نمایش لیست کامل میان‌برها  

- **خلاصهٔ دستورات پرکاربرد**  
  - جدول دستورات ساخت و مدیریت نشست‌ها  
  - جدول دستورات پیش‌فرض برای نشست، پنجره و پنل  


### **[بخش 04: نصب tmux به روش‌های مختلف](04-نصب_تی_ماکس_به_روش_های_مختلف.md)**